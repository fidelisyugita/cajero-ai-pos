name: Release Frontend Web App

on:
  push:
    tags:
      - "frontend-v*"
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Release tag name (e.g. frontend-v1.0.0)"
        required: true
        type: string
      draft:
        description: "Create release as draft"
        required: false
        type: boolean
        default: false
      prerelease:
        description: "Mark release as prerelease"
        required: false
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: release-frontend-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    name: Verify Frontend Codebase
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "yarn"
          cache-dependency-path: frontend/yarn.lock

      - name: Install dependencies
        run: yarn install --frozen-lockfile || yarn install

      - name: ESLint Check
        run: yarn lint

      - name: Vite Production Build
        run: yarn build

  create-release:
    name: Create GitHub Release
    needs: [verify]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Determine Tag and Version
        id: tag-info
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ inputs.tag_name }}"
          else
            TAG="${GITHUB_REF#refs/tags/}"
          fi
          echo "tag_name=$TAG" >> $GITHUB_OUTPUT
          VERSION="${TAG#frontend-v}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Generate Rich Release Notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          node ./scripts/generate-release-notes.js \
            --workspace frontend \
            --tag "${{ steps.tag-info.outputs.tag_name }}" \
            --output ./release-notes.md

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.tag-info.outputs.tag_name }}"
          TITLE="Frontend Web v${{ steps.tag-info.outputs.version }}"
          FLAGS=""
          if [ "${{ inputs.draft }}" = "true" ]; then
            FLAGS="$FLAGS --draft"
          fi
          if [ "${{ inputs.prerelease }}" = "true" ]; then
            FLAGS="$FLAGS --prerelease"
          fi

          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Updating existing release $TAG..."
            gh release edit "$TAG" --title "$TITLE" --notes-file ./release-notes.md $FLAGS
          else
            echo "Creating new release $TAG..."
            gh release create "$TAG" --title "$TITLE" --notes-file ./release-notes.md $FLAGS
          fi
